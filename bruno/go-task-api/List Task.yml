info:
  name: List Task
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:8080/tasks
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
