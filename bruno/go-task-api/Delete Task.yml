info:
  name: Delete Task
  type: http
  seq: 9

http:
  method: DELETE
  url: http://localhost:8080/tasks/{id}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
