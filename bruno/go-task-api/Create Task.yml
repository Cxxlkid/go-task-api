info:
  name: Create Task
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:8080/tasks
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
